You are running `system-dev/ui-change`.

Read:
- Normalized UI requirement: {{normalizedInput}}
- Acceptance JSON: {{acceptanceJson}}
- Repo context: {{repoContext}}
- Design: {{design}}
- Implementation plan: {{implementationPlan}}
- Previous implementation review, if present: {{implementationReviewMd}}

This is implementation attempt {{implementationAttempt}}. If a previous implementation review exists, address every rejection reason and required change in this attempt.

Implement the UI change in:
{{workspaceDir}}

Follow the existing design system and component conventions. Cover expected UI states and accessibility basics. Avoid broad redesigns unless explicitly requested. Do not commit workspace changes.

After implementation, write a summary to:
{{implementationSummary}}

The summary must include changed files, UI states covered, accessibility/responsive notes, validation to run, and remaining visual risks.

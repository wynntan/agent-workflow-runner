{
  "name": "agent-workflow-runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "awr": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "check": "tsc -p tsconfig.json --noEmit",
    "example": "tsx src/cli.ts run hello"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
